export const DEFAULT_REGISTRY_BASES = ['https://registry.npmjs.org/'] as const;
export const DEFAULT_PREFIX = '/vendor/npm/';
export const DEFAULT_CONDITIONS = ['browser', 'import', 'default'] as const;
